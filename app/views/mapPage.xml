<Page xmlns:maps="tns-googleMaps" navigatedTo="onNavigatedTo">
    <GridLayout rows="auto, *">
        <Label row="0" text="{{ location, 'lat: ' + location.latitude + ', long: ' + location.longitude + ', alt: ' + location.altitude }}"/>
        <maps:TnsGoogleMaps row="1" marker="{{ location }}" mapLoaded="onMapLoaded" id="map"/>
    </GridLayout>
</Page>
